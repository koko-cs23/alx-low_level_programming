#include <stdio.h>
/**
 * main - prints the phrase in the puts function
 * Return: 0 if no error otherwise returns non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

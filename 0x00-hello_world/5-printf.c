#include <stdio.h>
/**
 * main - prints the phrase in the function call
 * Return: 0 if no errors, return non zero if there are errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
